import { getInfoData } from "./dateInfo";
import { filterRemindersByDay } from "./filterRemindersByDay";
import { dateNavigator } from "./dateNavigator";

const utils = {
    getInfoData,
    dateNavigator,
    filterRemindersByDay
}

export default utils;